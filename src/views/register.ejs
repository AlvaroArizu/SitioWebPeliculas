<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie add</title>
    <link rel='stylesheet' href='/css/register.css' />
</head>

<body>
    <main>
        <form class="register-form" action="/register" method="post" id="form-register">
            <h3 class="register-title">Crear nueva cuenta</h3>

            <div class="form-group">
                <label for="name">Nombre:</label>
                <input type="text" id="name" name="name" placeholder="Nombres" class="register-input"
                    value="<%= locals.oldBody && oldBody.name ? oldBody.name : '' %>">
            </div>

            <div class="form-group">
                <label for="email">Correo Electrónico:</label>
                <input type="email" id="email" name="email" placeholder="Correo Electrónico" class="register-input"
                    value="<%= locals.oldBody && oldBody.email ? oldBody.email : '' %>">
            </div>

            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" class="register-input"
                    placeholder="Ingresar contraseña">
            </div>

            <% if (locals.isAdmin) { %>
            <div class="form-group">
                <label for="category">Categoría:</label>
                <select name="category" id="category">
                    <option selected disabled>Selecciona una Categoría</option>
                    <option value="2">User</option>
                    <option value="1">Admin</option>
                </select>
            </div>
            <% } %>

            <button class="submit" type="submit" id="btn-register">Crear cuenta</button>
        </form>
    </main>
</body>

</html>
